export type Task = {
    _id: string,
    title: string,
    description: string,
    completed: boolean,
    createdAt: string,
    updatedAt: string,
    __v: number
}


// interface RootState {
//     filteredTasks: Task[]; // Ajusta `Task` al tipo de tus tareas
//     loading: boolean;
// }
  